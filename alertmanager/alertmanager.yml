route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 2h
  receiver: 'stdout'

receivers:
  - name: 'stdout'
    webhook_configs:
      - url: 'http://prometheus:9090/-/ready'  # placeholder; replace with real webhook, Slack, etc.
